<% if !@answer.errors.present? %>
  $('<%= "#answer-#{@answer.id}" %>').html('<%= escape_javascript(render @answer) %>')
<% else %>
	$('<%= "#edit-answer-#{@answer.id}" %>').prepend('<%= escape_javascript(render 'shared/errors', resource: @answer) %>')
<% end %>