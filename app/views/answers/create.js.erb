$('.answer-errors').html('<%= escape_javascript(render 'shared/errors', resource: @answer) %>'); // it's always break line in response and got error without escape_javascript

<% if @answer.persisted? %>
  $('.new-answer #answer_body').val('');

  $('#links :input').each(function(index) {
    this.value = "";
  })
<% end %>